package mc.monacotelecom.services.enums;

/**
 * P (package de base)
 * O (option)
 * E (équipements
 * R (ressources)
 * U (use case)
 */
public enum ProvisioningTagNature {
    A, C, P, O, E, R, U
}
